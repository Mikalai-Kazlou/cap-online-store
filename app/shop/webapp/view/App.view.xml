<mvc:View
    controllerName="ns.shop.controller.App"
    displayBlock="true"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
>
    <Shell>
        <App
            id="app"
            class="myOnlineStoreSAPUI5"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"
        />
    </Shell>
</mvc:View>
